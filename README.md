# GraphOS
GraphOS is a custom experimental OS built around a graph-based architecture, where processes, resources, services, devices, and applications are represented as interconnected nodes. The goal is to make system relationships and dependencies first-class concepts across the entire operating system.
